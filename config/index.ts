import { default as DatabaseConfig } from "./database.config";
import { default as ServerConfig } from "./server.config";
import { default as IPFSConfig } from "./ipfs.config";

export { DatabaseConfig, ServerConfig, IPFSConfig};

export default { DatabaseConfig, ServerConfig, IPFSConfig };
