import { default as DatabaseConfig } from "./database.config";
import { default as ServerConfig } from "./server.config";

export { DatabaseConfig, ServerConfig};

export default { DatabaseConfig, ServerConfig };
